/**
 * Originally those tags were used. Now the are inlined so it compresses better. Still, leaving it here so you can check what each of them references.
 */
export const enum TAG {
    PLAYER = "p",
    ENEMY = "e",
    INTERACTIVE = "i",
    LIGHT = "l",
    GENERATED = "g"
}